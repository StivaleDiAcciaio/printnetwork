angular.module('tellmeApp',['ngSanitize', 'ui.bootstrap', 'ui.router', 'nya.bootstrap.select' ])
     .config([ function() {
  //// provider-injector
  // This is an example of config block.
  // You can have as many of these as you want.
  // You can only inject Providers (not instances)
  // into config blocks.
}]);